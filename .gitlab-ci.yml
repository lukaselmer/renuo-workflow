# This file is generated by GitLab CI
NPM Test:
  script:
  - npm install
  - bundle install
  - tsd reinstall --save --overwrite
  - npm test
    #  - "#bundle exec gem install compass-rails -- --use-system-libraries"
    #  - "#gem install compass-rails -- --use-system-libraries"
    #  - "#gem uninstall compass-rails"
    #  - "#gem install compass"
    #  - "#gem cleanup compass"
    #  - "#which compass"
    #  - "#rbenv which compass"
    #  - "#compass --version"
  tags: 
  except:
  - tags
